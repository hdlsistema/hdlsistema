import { Card } from '../../components/ui/Card'
import { SectionTitle } from '../../components/ui/SectionTitle'

export function Promotions() {
  return (
    <div className="space-y-6">
      <SectionTitle title="Promociones" subtitle="Pantalla vacía para campañas, bundles y descuentos." />
      <Card title="Centro de promociones" subtitle="Sin conexión a backend ni APIs externas." />
    </div>
  )
}
