import { Card } from '../../components/ui/Card'
import { SectionTitle } from '../../components/ui/SectionTitle'

export function Customers() {
  return (
    <div className="space-y-6">
      <SectionTitle title="Clientes" subtitle="Placeholder para CRM ligero y segmentación." />
      <Card title="Base de clientes" subtitle="Componente vacío con título visible." />
    </div>
  )
}
