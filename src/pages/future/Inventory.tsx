import { Card } from '../../components/ui/Card'
import { SectionTitle } from '../../components/ui/SectionTitle'

export function Inventory() {
  return (
    <div className="space-y-6">
      <SectionTitle title="Inventario" subtitle="Módulo futuro para stock, botellas y consumibles." />
      <Card title="Inventario futuro" subtitle="Estructura vacía preparada para diseño posterior." />
    </div>
  )
}
