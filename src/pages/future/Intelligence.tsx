import { Card } from '../../components/ui/Card'
import { SectionTitle } from '../../components/ui/SectionTitle'

export function Intelligence() {
  return (
    <div className="space-y-6">
      <SectionTitle title="Intelligence" subtitle="Módulo futuro para insights, scoring y tendencias." />
      <Card title="Intelligence" subtitle="Contenedor vacío con navegación ya conectada." />
    </div>
  )
}
